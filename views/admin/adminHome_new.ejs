<div class="row m-0">
    <!-- Sidebar -->
    <div class="col-2 p-0">
        <div class="sidebar text-white p-3" style="background-color: #654321; height: 100vh; position: fixed; width: 16.666%;">
            <div class="list-group list-group-flush">
                <a href="/admin" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #8B4513; border: none;">
                    <i class="bi bi-house me-2"></i>Home
                </a>
                <a href="/admin/book" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #7d5a32; border: none;">
                    <i class="bi bi-book me-2"></i>Books
                </a>
                <a href="/admin/author" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #7d5a32; border: none;">
                    <i class="bi bi-person me-2"></i>Authors
                </a>
                <a href="/admin/publisher" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #7d5a32; border: none;">
                    <i class="bi bi-building me-2"></i>Publishers
                </a>
                <a href="/admin/order" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #7d5a32; border: none;">
                    <i class="bi bi-cart me-2"></i>Orders
                </a>
                <a href="/admin/voucher" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #7d5a32; border: none;">
                    <i class="bi bi-tag me-2"></i>Vouchers
                </a>
                                <a href="/admin/users" class="list-group-item list-group-item-action text-white mb-2 rounded" style="background-color: #7d5a32; border: none;">
                    <i class="bi bi-people me-2"></i>Users
                </a>
            </div>
        </div>
    </div>
    
    <!-- Main Content -->
    <div class="col-10 offset-2">
        <div class="container-fluid py-3">
            <!-- Header -->
            <div class="row mb-4">
                <div class="col-12">
                    <h2 class="text-brown fw-bold"><i class="bi bi-graph-up me-2"></i>Admin Dashboard</h2>
                    <p class="text-muted">Welcome back! Here's what's happening with your bookstore today.</p>
                </div>
            </div>

            <!-- Summary Statistics Cards -->
            <div class="row g-3 mb-4">
                <!-- Total Earnings (All Time) -->
                <div class="col-lg-3 col-md-6">
                    <div class="card border-0 shadow-sm border-start border-4 border-success">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="me-3">
                                    <div class="bg-success bg-opacity-10 rounded-circle p-3">
                                        <i class="bi bi-currency-dollar text-success fs-4"></i>
                                    </div>
                                </div>
                                <div>
                                    <h6 class="card-title text-muted mb-1">Total Earnings</h6>
                                    <h3 class="mb-0 text-success">৳<%=totalEarnings.total_earnings || 0%></h3>
                                    <small class="text-muted">All time</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Total Orders -->
                <div class="col-lg-3 col-md-6">
                    <div class="card border-0 shadow-sm border-start border-4 border-primary">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="me-3">
                                    <div class="bg-primary bg-opacity-10 rounded-circle p-3">
                                        <i class="bi bi-cart text-primary fs-4"></i>
                                    </div>
                                </div>
                                <div>
                                    <h6 class="card-title text-muted mb-1">Total Orders</h6>
                                    <h3 class="mb-0 text-primary"><%=orderStats.total_orders || 0%></h3>
                                    <small class="text-success"><%=orderStats.completed_orders || 0%> completed</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Total Users -->
                <div class="col-lg-3 col-md-6">
                    <div class="card border-0 shadow-sm border-start border-4 border-info">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="me-3">
                                    <div class="bg-info bg-opacity-10 rounded-circle p-3">
                                        <i class="bi bi-people text-info fs-4"></i>
                                    </div>
                                </div>
                                <div>
                                    <h6 class="card-title text-muted mb-1">Registered Users</h6>
                                    <h3 class="mb-0 text-info"><%=totalUsers.total_users || 0%></h3>
                                    <small class="text-muted">Active customers</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Total Books -->
                <div class="col-lg-3 col-md-6">
                    <div class="card border-0 shadow-sm border-start border-4 border-warning">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="me-3">
                                    <div class="bg-warning bg-opacity-10 rounded-circle p-3">
                                        <i class="bi bi-book text-warning fs-4"></i>
                                    </div>
                                </div>
                                <div>
                                    <h6 class="card-title text-muted mb-1">Books Listed</h6>
                                    <h3 class="mb-0 text-warning"><%=inventoryStats.total_books || 0%></h3>
                                    <small class="text-danger"><%=inventoryStats.out_of_stock_books || 0%> out of stock</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Secondary Stats Row -->
            <div class="row g-3 mb-4">
                <div class="col-lg-2 col-md-4 col-sm-6">
                    <div class="card border-0 shadow-sm text-center">
                        <div class="card-body py-3">
                            <i class="bi bi-person-check text-primary fs-2"></i>
                            <h5 class="mt-2 mb-1"><%=inventoryStats.total_authors || 0%></h5>
                            <small class="text-muted">Authors</small>
                        </div>
                    </div>
                </div>
                <div class="col-lg-2 col-md-4 col-sm-6">
                    <div class="card border-0 shadow-sm text-center">
                        <div class="card-body py-3">
                            <i class="bi bi-building text-info fs-2"></i>
                            <h5 class="mt-2 mb-1"><%=inventoryStats.total_publishers || 0%></h5>
                            <small class="text-muted">Publishers</small>
                        </div>
                    </div>
                </div>
                <div class="col-lg-2 col-md-4 col-sm-6">
                    <div class="card border-0 shadow-sm text-center">
                        <div class="card-body py-3">
                            <i class="bi bi-tags text-success fs-2"></i>
                            <h5 class="mt-2 mb-1"><%=inventoryStats.total_genres || 0%></h5>
                            <small class="text-muted">Categories</small>
                        </div>
                    </div>
                </div>
                <div class="col-lg-2 col-md-4 col-sm-6">
                    <div class="card border-0 shadow-sm text-center">
                        <div class="card-body py-3">
                            <i class="bi bi-clock text-warning fs-2"></i>
                            <h5 class="mt-2 mb-1"><%=orderStats.pending_orders || 0%></h5>
                            <small class="text-muted">Pending Orders</small>
                        </div>
                    </div>
                </div>
                <div class="col-lg-2 col-md-4 col-sm-6">
                    <div class="card border-0 shadow-sm text-center">
                        <div class="card-body py-3">
                            <i class="bi bi-truck text-info fs-2"></i>
                            <h5 class="mt-2 mb-1"><%=orderStats.pending_deliveries || 0%></h5>
                            <small class="text-muted">Pending Deliveries</small>
                        </div>
                    </div>
                </div>
                <div class="col-lg-2 col-md-4 col-sm-6">
                    <div class="card border-0 shadow-sm text-center">
                        <div class="card-body py-3">
                            <i class="bi bi-exclamation-triangle text-danger fs-2"></i>
                            <h5 class="mt-2 mb-1"><%=inventoryStats.low_stock_books || 0%></h5>
                            <small class="text-muted">Low Stock</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Most Ordered Book -->
            <% if (mostOrderedBook && mostOrderedBook.name !== 'No orders yet') { %>
            <div class="row mb-4">
                <div class="col-12">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title"><i class="bi bi-trophy text-warning me-2"></i>Most Ordered Book of All Time</h5>
                            <div class="d-flex align-items-center">
                                <img src="<%=mostOrderedBook.image%>" alt="<%=mostOrderedBook.name%>" class="img-thumbnail me-3" style="width: 80px; height: 100px; object-fit: cover;">
                                <div>
                                    <h6 class="mb-1"><%=mostOrderedBook.name%></h6>
                                    <p class="text-muted mb-1">Total orders: <span class="fw-bold text-success"><%=mostOrderedBook.total_ordered%></span></p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>

            <!-- Order Management & Reports Row -->
            <div class="row g-3 mb-4">
                <!-- New Orders -->
                <div class="col-lg-6">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-bell text-danger me-2"></i>New Orders (Pending Approval)</h5>
                        </div>
                        <div class="card-body">
                            <% if (newOrders && newOrders.length > 0) { %>
                                <% newOrders.forEach(order => { %>
                                <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                                    <div>
                                        <h6 class="mb-1">Order #<%=order.id%></h6>
                                        <small class="text-muted"><%=order.customer_name%> - ৳<%=order.total_amount%></small>
                                        <br><small class="text-muted"><%=new Date(order.created_at).toLocaleDateString()%></small>
                                    </div>
                                    <div>
                                        <a href="/admin/order" class="btn btn-sm btn-success me-1">
                                            <i class="bi bi-check"></i>
                                        </a>
                                        <a href="/admin/order" class="btn btn-sm btn-danger">
                                            <i class="bi bi-x"></i>
                                        </a>
                                    </div>
                                </div>
                                <% }) %>
                                <div class="text-center mt-3">
                                    <a href="/admin/order" class="btn btn-outline-primary btn-sm">View All Orders</a>
                                </div>
                            <% } else { %>
                                <div class="text-center text-muted py-4">
                                    <i class="bi bi-inbox fs-1"></i>
                                    <p class="mt-2">No new orders</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>

                <!-- Top Bestselling Books -->
                <div class="col-lg-6">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-star text-warning me-2"></i>Top 5 Bestselling Books</h5>
                        </div>
                        <div class="card-body">
                            <% if (topBestsellingBooks && topBestsellingBooks.length > 0) { %>
                                <% topBestsellingBooks.forEach((book, index) => { %>
                                <div class="d-flex align-items-center mb-3">
                                    <span class="badge bg-primary rounded-circle me-3" style="width: 30px; height: 30px; display: flex; align-items: center; justify-content: center;"><%=index + 1%></span>
                                    <img src="<%=book.image%>" alt="<%=book.name%>" class="img-thumbnail me-3" style="width: 40px; height: 50px; object-fit: cover;">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1 small"><%=book.name%></h6>
                                        <small class="text-muted"><%=book.total_sold%> sold - ৳<%=book.price%></small>
                                    </div>
                                </div>
                                <% }) %>
                            <% } else { %>
                                <div class="text-center text-muted py-4">
                                    <i class="bi bi-graph-down fs-1"></i>
                                    <p class="mt-2">No sales data</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Charts Row -->
            <div class="row g-3 mb-4">
                <!-- Monthly Sales Chart -->
                <div class="col-lg-8">
                    <div class="card border-0 shadow-sm">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-graph-up me-2"></i>Last 30 Days Earnings</h5>
                        </div>
                        <div class="card-body">
                            <div id="curve_chart" style="min-height: 400px;"></div>
                        </div>
                    </div>
                </div>

                <!-- Notifications Panel -->
                <div class="col-lg-4">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-bell text-primary me-2"></i>Notifications</h5>
                        </div>
                        <div class="card-body">
                            <!-- Low Stock Alerts -->
                            <% if (lowStockBooks && lowStockBooks.length > 0) { %>
                            <div class="alert alert-warning py-2 px-3 mb-3">
                                <h6 class="alert-heading mb-2"><i class="bi bi-exclamation-triangle me-1"></i>Low Stock Alert</h6>
                                <% lowStockBooks.slice(0, 3).forEach(book => { %>
                                <small class="d-block">• <%=book.name%> (<%=book.stock%> left)</small>
                                <% }) %>
                                <% if (lowStockBooks.length > 3) { %>
                                <small class="text-muted">+<%=lowStockBooks.length - 3%> more books</small>
                                <% } %>
                            </div>
                            <% } %>

                            <!-- New User Signups -->
                            <% if (recentSignups && recentSignups.length > 0) { %>
                            <div class="alert alert-info py-2 px-3 mb-3">
                                <h6 class="alert-heading mb-2"><i class="bi bi-person-plus me-1"></i>New Signups (7 days)</h6>
                                <% recentSignups.slice(0, 3).forEach(user => { %>
                                <small class="d-block">• <%=user.name%></small>
                                <% }) %>
                                <% if (recentSignups.length > 3) { %>
                                <small class="text-muted">+<%=recentSignups.length - 3%> more users</small>
                                <% } %>
                            </div>
                            <% } %>

                            <!-- Recent Payments -->
                            <% if (recentPayments && recentPayments.length > 0) { %>
                            <div class="alert alert-success py-2 px-3 mb-0">
                                <h6 class="alert-heading mb-2"><i class="bi bi-credit-card me-1"></i>Recent Payments</h6>
                                <% recentPayments.slice(0, 3).forEach(payment => { %>
                                <small class="d-block">• Order #<%=payment.order_id%> - ৳<%=payment.total_amount%></small>
                                <% }) %>
                            </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Order Management Tables -->
            <div class="row g-3 mb-4">
                <!-- Recently Shipped -->
                <div class="col-lg-6">
                    <div class="card border-0 shadow-sm">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-truck text-success me-2"></i>Recently Shipped Orders</h5>
                        </div>
                        <div class="card-body">
                            <% if (recentlyShippedOrders && recentlyShippedOrders.length > 0) { %>
                                <div class="table-responsive">
                                    <table class="table table-sm table-hover">
                                        <thead>
                                            <tr>
                                                <th>Order ID</th>
                                                <th>Customer</th>
                                                <th>Amount</th>
                                                <th>Shipped</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% recentlyShippedOrders.forEach(order => { %>
                                            <tr>
                                                <td>#<%=order.id%></td>
                                                <td><%=order.customer_name%></td>
                                                <td>৳<%=order.total_amount%></td>
                                                <td><small><%=new Date(order.updated_at).toLocaleDateString()%></small></td>
                                            </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                </div>
                            <% } else { %>
                                <div class="text-center text-muted py-4">
                                    <i class="bi bi-truck fs-1"></i>
                                    <p class="mt-2">No recent shipments</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>

                <!-- Cancelled Orders -->
                <div class="col-lg-6">
                    <div class="card border-0 shadow-sm">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-x-circle text-danger me-2"></i>Recent Cancellations</h5>
                        </div>
                        <div class="card-body">
                            <% if (cancelledOrders && cancelledOrders.length > 0) { %>
                                <div class="table-responsive">
                                    <table class="table table-sm table-hover">
                                        <thead>
                                            <tr>
                                                <th>Order ID</th>
                                                <th>Customer</th>
                                                <th>Amount</th>
                                                <th>Cancelled</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% cancelledOrders.forEach(order => { %>
                                            <tr>
                                                <td>#<%=order.id%></td>
                                                <td><%=order.customer_name%></td>
                                                <td>৳<%=order.total_amount%></td>
                                                <td><small><%=new Date(order.updated_at).toLocaleDateString()%></small></td>
                                            </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                </div>
                            <% } else { %>
                                <div class="text-center text-muted py-4">
                                    <i class="bi bi-check-circle fs-1 text-success"></i>
                                    <p class="mt-2">No recent cancellations</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Yearly Chart -->
            <div class="row mb-4">
                <div class="col-12">
                    <div class="card border-0 shadow-sm">
                        <div class="card-header bg-transparent border-0">
                            <h5 class="mb-0"><i class="bi bi-calendar3 me-2"></i>Yearly Earnings Overview</h5>
                        </div>
                        <div class="card-body">
                            <div id="curve_chart2" style="min-height: 400px;"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Chart Scripts -->
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script type="text/javascript">
    let monthlyEarnings = <%- JSON.stringify(monthlyEarnings) %>;
    let monthlyData = [];
    monthlyData.push(['Date','Earnings (৳)']);
    for(let i=0;i<monthlyEarnings.length;i++){
        let smallData = [monthlyEarnings[i].month+" "+ monthlyEarnings[i].day, parseInt(monthlyEarnings[i].total_earned)];
        monthlyData.push(smallData);
    }

    let yearlyEarnings = <%- JSON.stringify(yearlyEarnings) %>;
    let yearlyData = [];
    yearlyData.push(['Month','Earnings (৳)']);
    for(let i=0;i<yearlyEarnings.length;i++){
        let smallData = [yearlyEarnings[i].month, parseInt(yearlyEarnings[i].total_earned)];
        yearlyData.push(smallData);
    }

    google.charts.load('current', {'packages':['corechart']});
    google.charts.setOnLoadCallback(drawChart);

    function drawChart() {
        var data = google.visualization.arrayToDataTable(monthlyData);
        var data2 = google.visualization.arrayToDataTable(yearlyData);
        
        var options = {
            curveType: 'function',
            legend: { position: 'bottom' },
            backgroundColor: 'transparent',
            colors: ['#28a745'],
            hAxis: {
                titleTextStyle: {color: '#333'},
                textStyle: {color: '#666'}
            },
            vAxis: {
                titleTextStyle: {color: '#333'},
                textStyle: {color: '#666'},
                format: '৳#,###'
            }
        };

        var chart = new google.visualization.LineChart(document.getElementById('curve_chart'));
        var chart2 = new google.visualization.LineChart(document.getElementById('curve_chart2'));

        chart.draw(data, options);
        chart2.draw(data2, options);
    }
</script>
